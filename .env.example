# Optional: production site origin for QR/contact-card URL when `window` is not available (SSR).
# In Vercel: Project → Settings → Environment Variables → add for Production (and Preview if needed).
# Example: https://www.nexa-fix.com  (no trailing slash)
NEXT_PUBLIC_SITE_URL=
